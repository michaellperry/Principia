<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Principia.Courses.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Class="Principia.Courses.Views.ModuleHeaderView"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <DataTemplate x:Key="ClipHeaderDataTemplate">
        	<Grid>
        		<TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="{Binding Title}" VerticalAlignment="Top" Margin="5" FontSize="14.667"/>
        	</Grid>
        </DataTemplate>
    </UserControl.Resources>

    <ContentControl x:Name="LayoutRoot" d:DataContext="{d:DesignData /SampleData/ModuleHeaderViewModelSampleData.xaml}" VerticalAlignment="Top" HorizontalContentAlignment="Stretch">
        <StackPanel Background="#FF4B2348" Margin="5">
            <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="{Binding Title}" VerticalAlignment="Top" FontSize="14.667" Margin="5"/>
            <ItemsControl x:Name="itemsControl" Margin="15,0,0,0" VerticalAlignment="Top" ItemsSource="{Binding Clips}" ItemTemplate="{StaticResource ClipHeaderDataTemplate}"/>
        </StackPanel>
    </ContentControl>
</UserControl>
