<UserControl
    x:Class="Principia.Courses.Views.CourseListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Principia.Courses.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="569.5"
    d:DesignWidth="1215"
    DataContext="{Binding CourseList, Source={StaticResource Locator}}">
	<UserControl.Resources>
		<DataTemplate x:Key="CourseViewModelTemplate">
			<Border Background="#FF0D3E14" Width="360" VerticalAlignment="Top" Height="240">
				<StackPanel>
					<TextBlock Text="{Binding Title}" TextWrapping="Wrap" FontSize="26.667" Margin="10"/>
					<TextBlock Text="{Binding Description}" TextWrapping="Wrap" FontSize="14.667" Margin="10" Foreground="{ThemeResource ApplicationSecondaryForegroundThemeBrush}"/>
				</StackPanel>
			</Border>
		</DataTemplate>
	</UserControl.Resources>
    
    <ScrollViewer VerticalScrollMode="Disabled" HorizontalScrollMode="Auto" ZoomMode="Disabled" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
		<GridView ItemsSource="{Binding Courses}" ItemTemplate="{StaticResource CourseViewModelTemplate}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Margin="120,0,0,60"/>
	</ScrollViewer>
</UserControl>
