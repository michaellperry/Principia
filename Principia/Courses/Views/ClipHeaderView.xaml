<UserControl
    x:Class="Principia.Courses.Views.ClipHeaderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Principia.Courses.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Loaded="UserControl_Loaded"
    Unloaded="UserControl_Unloaded">

    <Grid Background="Transparent" Tapped="Clip_Tapped">
        <Border x:Name="SelectedBorder" Background="{StaticResource SelectedModuleBackgroundBrush}"  Margin="0,0,-20,0" IsHitTestVisible="False" />
        <TextBlock HorizontalAlignment="Left" Text="{Binding Title}" VerticalAlignment="Top" Margin="5" FontSize="26.667"/>
    </Grid>
</UserControl>
